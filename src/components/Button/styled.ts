import styled from 'styled-components';

export const ButtonWrapper = styled.button`
  padding: 0.4rem 0.8rem;
  border-radius: 3px;
  border: 0.1rem solid var(--rebecca-purple);
  background: var(--rebecca-purple)
  color: white;
`;
